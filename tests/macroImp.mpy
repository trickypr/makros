macro import enum

enum Test:
    A
    B(val1: str, val2: str)
    frog

enum my_words:
    frog
    bob
    lion

test1 = Test.B('one', 'two')
print(isinstance(test1, Test.B))
print(test1 == Test.B)
example = my_words.frog()

print(test1)
print(example)